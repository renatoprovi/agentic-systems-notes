[project]
name = "mcp-toy-server"
version = "0.1.0"
description = "Toy MCP server exposing this repo's own reading notes as Resources and a search Tool"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=2.2.0",
]
